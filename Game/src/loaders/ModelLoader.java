package loaders;

import mini.scene.Node;
import mini.scene.Spatial;
import mini.utils.MyFile;

public class ModelLoader {
/*    protected Node loadModel(MyFile modelFile) {
        //Spatial data = ObjFileLoader.loadOBJ(modelFile);
        return (Node) data;
//        VAO vao = VAO.create();
//        vao.storeData(data.getIndices(), data.getVertexCount(), data.getVertices(),
//                      data.getTextureCoords(), data.getNormals());
//        return new Geometry(vao);
    }*/
}
