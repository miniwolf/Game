package loaders;

import mini.material.Material;
import mini.textures.Texture;
import mini.utils.MyFile;

public class SkinLoader {
//    protected Material loadSkin(MyFile diffuseFile){
//        Texture diffuse = Texture.newTexture(diffuseFile).anisotropic().create();
//        return new Material(diffuse, null);
//    }
//
//    protected Material loadSkin(MyFile diffuseFile, MyFile extraMapFile){
//        Texture diffuse = Texture.newTexture(diffuseFile).anisotropic().create();
//        Texture extraMap = Texture.newTexture(extraMapFile).create();
//        return new Material(diffuse, extraMap);
//    }
}
